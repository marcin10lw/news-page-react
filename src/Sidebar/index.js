import { StyledSidebar, Heading } from "./styled";
import { Flex } from "../Flex/styled";

const Sidebar = () => (
  <StyledSidebar>
    <Flex>
      <Heading>New</Heading>
    </Flex>
  </StyledSidebar>
);

export default Sidebar;
